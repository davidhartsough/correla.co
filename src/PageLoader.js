import React from "react";
import Loader from "./Loader";

function PageLoader() {
  return (
    <div className="page-loader">
      <Loader size={5} />
    </div>
  );
}

export default PageLoader;
